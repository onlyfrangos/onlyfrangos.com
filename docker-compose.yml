services:
  postgres:
    image: postgres:16-alpine
    container_name: onlyfrangos-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-onlyfrangos}
      POSTGRES_USER: ${POSTGRES_USER:-onlyfrangos}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-onlyfrangos}
    ports:
      - '${POSTGRES_PORT:-5432}:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test:
        ['CMD-SHELL', 'pg_isready -U ${POSTGRES_USER:-onlyfrangos} -d ${POSTGRES_DB:-onlyfrangos}']
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: onlyfrangos-redis
    restart: unless-stopped
    ports:
      - '${REDIS_PORT:-6379}:6379'
    command: ['redis-server', '--appendonly', 'yes']
    volumes:
      - redis_data:/data
    profiles: ['cache']

volumes:
  postgres_data:
  redis_data:
