{
  "name": "@onlyfrangos/config",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "echo 'config package: no build needed'",
    "lint": "echo 'config package: no lint target'",
    "test": "echo 'config package: no tests'",
    "format": "prettier --check ."
  },
  "devDependencies": {
    "prettier": "^3.4.2"
  }
}
